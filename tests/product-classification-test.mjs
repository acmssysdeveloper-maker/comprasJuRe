import assert from "node:assert/strict";import fs from "node:fs";const app=fs.readFileSync("app.js","utf8"),html=fs.readFileSync("index.html","utf8");assert.match(app,/const SECTORS=\[/);assert.match(app,/const HEALTH_CLASSES=\["Saudável","Não saudável","Não classificado"\]/);assert.match(app,/npHealth/);assert.match(app,/if\(!health\)return toast\("Selecione Saudável ou Não saudável\."\)/);assert.match(app,/sectorSnapshot/);assert.match(app,/healthSnapshot/);assert.match(html,/id="productSectorFilter"/);assert.match(html,/option>Saudável/);assert.match(html,/option>Não saudável/);console.log("Product classification/sector checks: PASS");
