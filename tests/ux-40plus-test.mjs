import assert from "node:assert/strict";import fs from "node:fs";const css=fs.readFileSync("styles.css","utf8");const html=fs.readFileSync("index.html","utf8");assert.match(css,/font-family:"Segoe UI",Inter/);assert.match(css,/body\{font-size:15px/);assert.match(css,/font-size:16px/);assert.match(css,/min-height:46px/);assert.match(css,/market-session/);assert.match(fs.readFileSync("app.js","utf8"),/placeholder="Buscar produto pelo nome"/);console.log("UX 40+ readability checks: PASS");
